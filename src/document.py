class Document:
    def __init__(self, title: str, url: str, desc: str):
        self.title = title
        self.url = url
        self.desc = desc

        # Constructed with basic fields. Add more if needed
